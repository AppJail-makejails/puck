--- client.py.orig	2026-03-20 01:16:32.318489000 -0400
+++ client.py	2026-03-20 01:16:25.416151000 -0400
@@ -1,4 +1,4 @@
-#!/usr/bin/python3
+#!/usr/bin/python%%PYVER%%
 # -*- coding: utf-8 -*-
 
 # The Qubes OS Project, http://www.qubes-os.org
@@ -36,7 +36,7 @@
 from PIL import Image
 from tempfile import TemporaryDirectory
 
-CLIENT_VM_CMD = ["/usr/bin/qrexec-client-vm", "@dispvm", "qubes.PdfConvert"]
+CLIENT_VM_CMD = ["python%%PYVER%%", "server.py", ""]
 
 MAX_PAGES = 10000
 MAX_IMG_WIDTH = 10000
@@ -583,14 +583,14 @@
     def _archive(self, archive):
         """Move original file into an archival directory"""
         Path.mkdir(archive, exist_ok=True)
-        self.path.rename(Path(archive, self.path.name))
+        shutil.move(self.path, Path(archive, self.path.name))
 
 
 async def run(params):
     CLIENT_VM_CMD[-1] += "+" + str(params["resolution"])
     BaseFile.output_resolution = params["resolution"]
     suffix = "s" if len(params["files"]) > 1 else ""
-    print(f"Sending file{suffix} to Disposable VM{suffix}...\n")
+    print(f"Sending file{suffix} to Ephemeral Jail{suffix}...\n")
 
     tasks = []
     jobs = [Job(f, i) for i, f in enumerate(params["files"])]
